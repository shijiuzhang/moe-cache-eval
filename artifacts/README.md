# Payload boundary

The public repository does not mirror raw model routes, prompt text, model
weights, or local dataset caches. Their provenance and redistribution status are
tracked in `paper/ARTIFACT_LICENSE_MATRIX.md`.

The paper can be inspected from the released aggregate CSV/JSON artifacts. Full
trace regeneration uses the scripts in `scripts/` and the frozen input
identifiers recorded by the corresponding builders.

